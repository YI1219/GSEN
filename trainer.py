import torch
import torch.nn as nn


class GlobalUpdate():
    def __init__(self, configs, dataset, id) -> None:
        self.id = id
        self.dataset = dataset
    
    def update():
        s
        

class LocalTrainer():
    def __init__(self, configs, dataset, id) -> None:
        self.id = id
        self.dataset = dataset

    def train(self, x, y):
        pass